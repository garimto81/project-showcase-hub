# PRD-0002: Aiden's Market - Personal App Marketplace

| 항목 | 값 |
|------|---|
| **Version** | 2.5.1 |
| **Status** | In Progress |
| **Priority** | P0 |
| **Created** | 2025-12-26 |
| **Updated** | 2026-01-07 |
| **License** | MIT |
| **Repository** | `D:\AI\claude01\project-showcase-hub` |

---

## 1. Executive Summary

### 1.1 배경
기존 Project Showcase Hub는 포트폴리오 관리 플랫폼으로 설계되었습니다.
사용자 요구에 따라 **개인용 앱 마켓/대시보드**로 목적을 전환하여,
자신이 개발하거나 사용하는 앱들을 한 곳에서 관리하고 빠르게 접근할 수 있는 플랫폼으로 발전시킵니다.

### 1.2 목표
- 개인용 앱 마켓/대시보드 플랫폼 구축
- 다양한 앱 유형 지원 (웹앱, PWA, API, Docker)
- 앱 런처와 스토어 듀얼 모드 제공

### 1.3 핵심 변화

| 기존 (v1.x - Project Showcase) | 신규 (v2.0 - Aiden's Market) |
|-------------------------------|---------------------|
| 프로젝트 포트폴리오 관리 | **개인용 앱 마켓/대시보드** |
| 프로젝트 (Project) | 앱 (App) |
| 정적 정보 표시 | **앱 런처 (원클릭 실행)** |
| 단순 뷰 모드 | **Dashboard + Store 듀얼 모드** |
| 카테고리 없음 | **카테고리별 분류** |
| URL 정보 없음 | **앱 URL, Docker 이미지 등 지원** |

### 1.4 참고 솔루션

| 솔루션 | 특징 | 참고 포인트 |
|--------|------|------------|
| [Homer](https://github.com/bastienwirtz/homer) | YAML 기반, 정적, PWA | 간결한 타일 레이아웃 |
| [Homarr](https://homarr.dev/) | 드래그앤드롭, 위젯 | UI 편집 경험 |
| [Heimdall](https://heimdall.site/) | 앱 타일, API 연동 | 상태 표시 |
| [Dashy](https://dashy.to/) | 50+ 위젯, 모니터링 | 커스터마이징 |
| [Cloudron](https://www.cloudron.io/) | 앱스토어 + 배포 | 앱 마켓 구조 |

---

## 2. Technology Stack

| 영역 | 기술 | 버전 | 선정 이유 |
|------|------|------|----------|
| **Framework** | Next.js | 16.x (App Router) | SSR, SEO, RSC |
| **Language** | TypeScript | 5.x | 타입 안정성 |
| **UI** | Tailwind CSS | v4 | 유틸리티 기반 |
| **Components** | shadcn/ui | latest (new-york) | 접근성, 커스터마이징 |
| **Database** | Supabase | PostgreSQL | 오픈소스, RLS |
| **Auth** | 환경변수 인증 | - | Admin 전용 (ADMIN_PASSWORD) (v2.4) |
| **GitHub API** | 공개 API | - | OAuth 불필요, /users/{username}/repos |
| **Deploy** | Vercel | - | Edge Network |

### 2.1 인증 시스템 단순화 및 익명 댓글/별점 지원 (v2.4)

**변경 배경**: v2.2에서 환경변수 인증으로 단순화했으나, 별점/댓글 작성 시 UUID 타입 에러 발생 (#31).
복잡한 이중 인증 시스템 대신, **로그인 없이도 누구나 별점과 댓글을 남길 수 있도록** 변경하여 사용자 경험을 개선.

| Role | 인증 방식 | 권한 |
|------|----------|------|
| **Guest** (비로그인) | 인증 불필요 | 앱 조회 + **별점/댓글 작성** ✨ |
| **Admin** | 환경변수 비밀번호 | 앱 CRUD + 설정 관리 |

> **Note (v2.5.1)**: OAuth 소셜 로그인(GitHub/Google)은 **미구현 상태**입니다.
> E2E 테스트에서 OAuth 관련 테스트는 skip 처리되어 있습니다.
> 현재는 Admin 비밀번호 로그인 + User 이메일/비밀번호 로그인만 지원합니다.

**환경변수**:
```bash
ADMIN_PASSWORD=your-secure-password
GITHUB_USERNAME=garimto81
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
```

**익명 작성 플로우**:
```typescript
// 익명 댓글 작성
POST /api/projects/{id}/comments
{
  "content": "댓글 내용",
  "author_name": "홍길동"  // 익명 작성자 이름 (필수)
}

// 익명 별점 등록
POST /api/projects/{id}/ratings
{
  "score": 5  // user_id는 자동으로 null 처리
}
```

**데이터베이스 변경**:
```sql
-- user_id nullable로 변경
ALTER TABLE comments ALTER COLUMN user_id DROP NOT NULL;
ALTER TABLE ratings ALTER COLUMN user_id DROP NOT NULL;

-- 익명 작성자 이름 필드 추가
ALTER TABLE comments ADD COLUMN author_name TEXT;
```

**주요 변경 사항 (v2.3 → v2.4)**:
- ✅ 별점/댓글 `user_id` nullable 처리 (익명 사용자 지원)
- ✅ 댓글 `author_name` 필드 추가 (익명 작성자 이름)
- ✅ `requireAuth()` 제거 (comments, ratings API)
- ✅ RLS 정책 업데이트 (익명 작성 허용)
- ✅ 단일 Admin 인증 유지 (기존 v2.2 방식)
- ❌ User 로그인/회원가입 제거 (불필요)

---

## 3. Target Users

### 3.1 Primary Users
- **개인 개발자**: 자신이 만든/사용하는 앱을 한 곳에서 관리
- **Self-hosted 운영자**: Docker 앱들의 런처로 활용
- **프리랜서/팀**: 팀 내부 도구 접근 허브

### 3.2 User Roles & Access Control

| Role | 권한 | 인증 필요 |
|------|------|:--------:|
| **Guest** (비로그인) | 앱 조회 + 별점/댓글 작성 (익명) | ❌ |
| **Admin** (관리자) | 앱 CRUD + 설정 관리 | ✅ |

#### 접근 권한 매트릭스

| 기능 | Guest | Admin |
|------|:-----:|:-----:|
| 앱 목록 조회 | ✅ | ✅ |
| 앱 상세 보기 | ✅ | ✅ |
| 앱 실행 (URL 열기) | ✅ | ✅ |
| 별점 보기 | ✅ | ✅ |
| 댓글 보기 | ✅ | ✅ |
| 별점 등록 (익명) | ✅ | ✅ |
| 댓글 작성 (익명, author_name 입력) | ✅ | ✅ |
| 댓글 수정/삭제 | ❌ | ❌ |
| 앱 추가/수정/삭제 | ❌ | ✅ |
| 즐겨찾기 관리 | ❌ | ✅ |
| 설정 관리 | ❌ | ✅ |
| GitHub 레포 스캔 | ❌ | ✅ |

> **설계 의도**:
> - **누구나 로그인 없이** 앱 마켓을 탐색하고, 별점/댓글을 남길 수 있습니다.
> - 익명 댓글은 작성자 이름(`author_name`)을 입력하여 작성합니다.
> - 익명 작성물은 수정/삭제가 불가능합니다 (스팸 방지는 Admin이 직접 관리).
> - 관리자만 앱을 등록하고 관리할 수 있습니다.

---

## 4. Core Features

### 4.1 듀얼 모드 시스템

#### Dashboard Mode (런처)
```
+--------------------------------------------------+
|  Aiden's Market                           [Store] [+]    |
+--------------------------------------------------+
|  [즐겨찾기 앱]                                    |
|  +------+ +------+ +------+ +------+             |
|  | [*]  | | [*]  | | [x]  | | [*]  |             |
|  | App1 | | App2 | | App3 | | App4 |             |
|  +------+ +------+ +------+ +------+             |
|                                                  |
|  [최근 사용]                                      |
|  +------+ +------+ +------+                      |
|  | App  | | App  | | App  |                      |
|  +------+ +------+ +------+                      |
|                                                  |
|  [카테고리별 빠른 접근]                           |
|  개발도구(5) | 생산성(3) | 유틸리티(8) | ...      |
+--------------------------------------------------+
```

**특징**:
- 즐겨찾기 앱 빠른 접근
- 최근 사용 앱 (사용 기록 추적)
- 상태 표시 (온라인/오프라인)
- 원클릭 앱 실행

#### Store Mode (스토어)
```
+--------------------------------------------------+
|  Aiden's Market Store                  [Dashboard] [+]   |
+--------------------------------------------------+
|  [검색]________________  [카테고리 v] [유형 v]    |
+--------------------------------------------------+
|  Sidebar           |  앱 목록 (Grid/List)        |
|  - 전체            |  +--------+ +--------+      |
|  - 개발 도구       |  | App    | | App    |      |
|  - 생산성          |  | * 4.5  | | * 4.2  |      |
|  - 미디어          |  | Docker | | Web    |      |
|  - 유틸리티        |  +--------+ +--------+      |
|  - AI/ML           |                             |
+--------------------------------------------------+
```

**특징**:
- 카테고리별 탐색
- 검색 및 필터링 (앱 타입, 태그)
- 평점/리뷰 표시
- 상세 정보 보기

### 4.2 앱 유형 지원

| 타입 | 설명 | 런치 동작 | 메타데이터 |
|------|------|----------|-----------|
| **web_app** | 배포된 웹앱 | URL 열기 | url, status |
| **pwa** | Progressive Web App | URL 열기 (설치 안내) | url, manifest_url |
| **api** | API/라이브러리 | 문서 열기 | api_endpoint, docs_url |
| **docker** | Docker 이미지 | 명령어 복사/실행 안내 | docker_image, compose_url |

### 4.3 카테고리 시스템

| 카테고리 | 아이콘 | 설명 |
|----------|--------|------|
| 개발 도구 | `code` | IDE, 버전관리, CI/CD |
| 생산성 | `zap` | 노트, 일정, 협업 |
| 미디어 | `play` | 스트리밍, 편집 |
| 유틸리티 | `wrench` | 파일관리, 시스템 |
| 모니터링 | `activity` | 서버, 네트워크 모니터링 |
| AI/ML | `brain` | AI 도구, ML 플랫폼 |
| 데이터베이스 | `database` | DB 관리 도구 |
| 기타 | `folder` | 분류되지 않은 앱 |

### 4.4 뷰 모드

| 뷰 | 설명 | 용도 |
|----|------|------|
| **Launcher** (신규) | 타일 그리드 | Dashboard 메인 |
| **Gallery** | 카드 그리드 | Store 기본 뷰 |
| **List** | 테이블 형태 | Store 상세 뷰 |
| **Board** | 카테고리별 칸반 | 카테고리 정리 |
| **Timeline** | 업데이트 순 | 최근 업데이트 확인 |

### 4.5 별점 & 리뷰 시스템 (익명 작성 지원)

#### 별점 시스템
- 1-5점 별점
- **익명 작성 가능** (로그인 불필요)
- 평균/분포 표시
- 중복 방지 없음 (나중에 IP 기반 제한 추가 가능)

#### 리뷰 시스템
- 댓글 작성, 조회
- **익명 작성 가능** (작성자 이름 `author_name` 입력)
- 익명 댓글 표시: "홍길동 (게스트)"
- 익명 작성물은 수정/삭제 불가
- 스팸 관리: Admin이 직접 삭제

**API 예시**:
```typescript
// 익명 댓글 작성
POST /api/projects/{id}/comments
{
  "content": "정말 유용한 앱이네요!",
  "author_name": "홍길동"
}

// 익명 별점 등록
POST /api/projects/{id}/ratings
{
  "score": 5
}
```

### 4.6 앱 상태 모니터링 (선택적)

```typescript
interface AppStatus {
  status: 'online' | 'offline' | 'unknown'
  lastChecked: string
  responseTime?: number // ms
}
```

- HEAD 요청으로 상태 확인
- 대시보드에 상태 dot 표시 (녹색=온라인, 빨강=오프라인, 회색=알수없음)

---

## 5. Data Model

### 5.1 ERD 변경

```sql
-- 카테고리 테이블 (신규)
categories
├── id (uuid, PK)
├── name (text)
├── slug (text, unique)
├── icon (text)               -- lucide icon name
├── description (text)
├── sort_order (int)
├── created_at (timestamptz)

-- 앱 타입 ENUM (신규)
CREATE TYPE app_type AS ENUM ('web_app', 'pwa', 'api', 'docker')

-- Projects → Apps 확장
projects (기존 테이블 확장)
├── id (uuid, PK)
├── title (text)
├── description (text)
├── owner_id (uuid, FK → profiles)
├── thumbnail_url (text)
├── created_at (timestamptz)
├── updated_at (timestamptz)
│
│ -- 신규 필드 추가 --
├── app_type (app_type)       -- 앱 유형
├── category_id (uuid, FK)    -- 카테고리
├── url (text)                -- 앱 URL
├── api_endpoint (text)       -- API 엔드포인트
├── docker_image (text)       -- Docker 이미지
├── docker_compose_url (text) -- docker-compose URL
├── version (text)            -- 버전
├── github_repo (text)        -- GitHub 레포 URL
├── documentation_url (text)  -- 문서 URL
├── is_favorite (boolean)     -- 즐겨찾기
├── last_accessed_at (timestamptz) -- 마지막 접속
├── status (text)             -- online/offline/unknown
├── tags (text[])             -- 태그 배열

-- 사용 기록 테이블 (신규)
app_usage
├── id (uuid, PK)
├── app_id (uuid, FK → projects)
├── user_id (uuid, FK → profiles)
├── accessed_at (timestamptz)
├── UNIQUE(app_id, user_id)

-- 기존 테이블 유지
ratings (기존 유지)
comments (기존 유지)
profiles (기존 유지)
```

### 5.2 TypeScript 타입

```typescript
// src/types/database.ts

export type AppType = 'web_app' | 'pwa' | 'api' | 'docker'
export type AppStatus = 'online' | 'offline' | 'unknown'

export interface Category {
  id: string
  name: string
  slug: string
  icon: string | null
  description: string | null
  sort_order: number
  created_at: string
}

export interface App {
  id: string
  title: string
  description: string | null
  owner_id: string
  thumbnail_url: string | null
  created_at: string
  updated_at: string
  // 신규 필드
  app_type: AppType
  category_id: string | null
  url: string | null
  api_endpoint: string | null
  docker_image: string | null
  docker_compose_url: string | null
  version: string | null
  github_repo: string | null
  documentation_url: string | null
  is_favorite: boolean
  last_accessed_at: string | null
  status: AppStatus
  tags: string[]
}

export interface AppWithCategory extends App {
  category: Category | null
  owner: Pick<Profile, 'id' | 'display_name' | 'avatar_url'> | null
}
```

---

## 6. Non-Functional Requirements

| 항목 | 요구사항 |
|------|----------|
| **Performance** | LCP < 2.5s, FID < 100ms |
| **Accessibility** | WCAG 2.1 AA 준수 |
| **Responsive** | Mobile-first, 320px ~ 1920px |
| **Security** | RLS, HTTPS Only |
| **i18n** | 한국어 (영어 추후) |

---

## 7. MVP (Minimum Viable Product)

### 7.1 MVP 목표

**핵심 전환**: GitHub 레포 수동 import → **전체 레포 자동 스캔 및 앱 마켓 자동 배치**

기존 Project Showcase Hub는 사용자가 레포를 하나씩 선택하여 import했습니다.
Aiden's Market MVP에서는 **GitHub 연동만 하면 모든 레포를 자동 스캔**하여 배포된 앱을 찾고, **마켓에 자동 배치**합니다.

| 기존 방식 | MVP 방식 |
|----------|----------|
| 레포 수동 선택/import | **전체 레포 자동 스캔** |
| 레포 메타데이터 표시 | **배포 URL 자동 탐지** |
| 수동으로 앱 등록 | **마켓에 자동 배치** |
| 프로젝트 정보 표시 | **앱 런처로 바로 실행** |

### 7.1.1 자동화 흐름

```
┌─────────────────────────────────────────────────────────────┐
│                     GitHub 계정 연동                         │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  1. 전체 레포 자동 스캔 (백그라운드)                          │
│     - 사용자의 모든 public/private 레포 조회                 │
│     - 각 레포별 배포 URL 탐지                                │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  2. 배포된 앱 자동 필터링                                    │
│     - 배포 URL이 있는 레포만 추출                            │
│     - URL 유효성 검증 (HEAD 요청)                           │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  3. 앱 마켓에 자동 배치                                      │
│     - 앱 정보 자동 생성 (이름, 설명, 썸네일)                  │
│     - 마켓에 자동 등록                                       │
│     - 사용자에게 결과 알림                                   │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  Aiden's Market 마켓                                                │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐                       │
│  │ App1 │ │ App2 │ │ App3 │ │ App4 │  ← 자동 배치 완료     │
│  └──────┘ └──────┘ └──────┘ └──────┘                       │
└─────────────────────────────────────────────────────────────┘
```

### 7.2 배포 URL 자동 탐지 방식

GitHub 레포를 분석하여 다음 순서로 배포된 앱 URL을 자동 탐지합니다:

```
GitHub 레포 입력
    ↓
┌─────────────────────────────────────────────────────────┐
│  1. GitHub API로 레포 정보 조회                          │
│     - homepage 필드 확인 (package.json 또는 레포 설정)    │
│     - GitHub Pages 활성화 여부 확인                      │
│     - 환경(Environments) 배포 URL 확인                   │
├─────────────────────────────────────────────────────────┤
│  2. README.md 분석                                       │
│     - Demo/Live 링크 패턴 탐지                           │
│     - 배지(Badge) URL 추출 (Vercel, Netlify 등)          │
├─────────────────────────────────────────────────────────┤
│  3. 배포 플랫폼 패턴 매칭                                 │
│     - vercel.app, netlify.app, github.io 등             │
│     - 레포 이름 기반 URL 추론                            │
└─────────────────────────────────────────────────────────┘
    ↓
배포 URL 목록 출력 (사용자 선택/확인)
```

#### 탐지 우선순위

| 순위 | 소스 | 방법 | 신뢰도 |
|:----:|------|------|:------:|
| 1 | GitHub API `homepage` | 레포 설정의 Website 필드 | 높음 |
| 2 | GitHub Pages | `https://{user}.github.io/{repo}` | 높음 |
| 3 | GitHub Environments | Deployments API 조회 | 높음 |
| 4 | README 링크 | "Demo", "Live", "Website" 패턴 | 중간 |
| 5 | README 배지 | Vercel/Netlify 배지 URL 추출 | 중간 |
| 6 | URL 추론 | `{repo}.vercel.app` 등 패턴 시도 | 낮음 |

#### 탐지 가능한 배포 플랫폼

| 플랫폼 | URL 패턴 | 탐지 방법 |
|--------|----------|----------|
| **GitHub Pages** | `{user}.github.io/{repo}` | GitHub API |
| **Vercel** | `{project}.vercel.app` | README 배지, Environments |
| **Netlify** | `{project}.netlify.app` | README 배지 |
| **Railway** | `{project}.up.railway.app` | Environments |
| **Render** | `{project}.onrender.com` | README 링크 |
| **Cloudflare Pages** | `{project}.pages.dev` | README 링크 |
| **Custom Domain** | 사용자 도메인 | `homepage` 필드 |

### 7.3 MVP UI 흐름 (Stitch 디자인 기반 v2.5)

> **디자인 파일**: `design/stitch_welcome_to_aiden_s_market.zip`
> **디자인 도구**: Stitch (모바일 우선 디자인)

#### 디자인 시스템

| 항목 | 값 |
|------|---|
| **테마** | 다크 모드 기본 (Dark Navy: `#0a1628`) |
| **액센트 컬러** | 시안 (Cyan: `#00d4ff`) |
| **카드 배경** | 반투명 다크 (`#1a2a3a` / `rgba(26,42,58,0.8)`) |
| **폰트** | System UI (SF Pro, Inter) |
| **아이콘** | Lucide Icons |
| **레이아웃** | 모바일 퍼스트, 2열 그리드 |

#### Step 1: Welcome 화면 (온보딩)

```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│              ┌───────────────────┐                      │
│              │    [자물쇠 이미지]  │                      │
│              │       + 로고       │                      │
│              └───────────────────┘                      │
│                                                         │
│          Welcome to Aiden's Market                      │
│                 (시안 컬러 강조)                          │
│                                                         │
│        Your personal app command center.                │
│     Connect your GitHub account to automatically        │
│   scan, register, and organize your deployed apps.      │
│                                                         │
│              ✓ Secure read-only access                  │
│                                                         │
│         ┌─────────────────────────────┐                 │
│         │  🔗 Link GitHub Account     │  ← 시안 버튼    │
│         └─────────────────────────────┘                 │
│                                                         │
│              Add apps manually                          │
│                                                         │
│    We only scan public repositories and active          │
│    deployments. Your private code remains private.      │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**핵심 요소**:
- 보안 강조 이미지 (자물쇠)
- "Secure read-only access" 신뢰 배지
- 주요 CTA: "Link GitHub Account" (시안 버튼)
- 대안 링크: "Add apps manually"
- 프라이버시 안내 텍스트

#### Step 2: Scanning 화면 (GitHub 스캔 중)

```
┌─────────────────────────────────────────────────────────┐
│  Scanning GitHub                              Cancel    │
├─────────────────────────────────────────────────────────┤
│                                                         │
│                   ┌──────────┐                          │
│                  /     45%    \                         │
│                 │   Complete   │  ← 원형 프로그레스      │
│                  \            /                         │
│                   └──────────┘                          │
│                                                         │
│         12 of 48 Repositories Analyzed                  │
│      ⚡ Checking: user/ecommerce-api...                 │
│                                                         │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Detected Apps                         3 Found    │  │
│  ├───────────────────────────────────────────────────┤  │
│  │  ┌──────┐  Portfolio Site              ✓          │  │
│  │  │[img] │  Vercel • Next.js Framework             │  │
│  │  └──────┘                                         │  │
│  │  ┌──────┐  Crypto Tracker API          ✓          │  │
│  │  │[img] │  AWS Lambda • Node.js                   │  │
│  │  └──────┘                                         │  │
│  │  ┌──────┐  Task Manager v2             ○          │  │
│  │  │[img] │  Identifying tech stack...   (로딩)     │  │
│  │  └──────┘                                         │  │
│  │  ┌──────┐  ████████████████            (스켈레톤)  │  │
│  │  │      │  ████████                               │  │
│  │  └──────┘                                         │  │
│  └───────────────────────────────────────────────────┘  │
│                                                         │
│         ┌─────────────────────────────┐                 │
│         │   Scanning in progress...   │  ← disabled    │
│         └─────────────────────────────┘                 │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**핵심 요소**:
- 원형 프로그레스 (퍼센트 표시)
- 실시간 분석 현황 ("12 of 48 Repositories")
- 현재 분석 중인 레포 표시
- 감지된 앱 실시간 목록 (썸네일 + 이름 + 배포처 + 프레임워크)
- 스켈레톤 로딩 상태

#### Step 3: Scan Complete 화면 (스캔 완료)

```
┌─────────────────────────────────────────────────────────┐
│  ✕                                                      │
├─────────────────────────────────────────────────────────┤
│                                                         │
│                    ┌─────┐                              │
│                    │  ✓  │  ← 시안 체크 아이콘          │
│                    └─────┘                              │
│                                                         │
│               Scan Complete!                            │
│                                                         │
│       We successfully synced your GitHub                │
│    repositories and registered your deployments.        │
│                                                         │
│  ┌───────────────────────────────────────────────────┐  │
│  │  NEW APPS ADDED                          :::      │  │
│  │       12                                 (그리드)  │  │
│  └───────────────────────────────────────────────────┘  │
│                                                         │
│  Registered Apps                              New       │
│                                                         │
│  ┌─────────────┐  ┌─────────────┐                      │
│  │             │  │             │                      │
│  │   [thumb]   │  │   [thumb]   │                      │
│  │             │  │             │                      │
│  │ Project     │  │ ToDo React  │                      │
│  │ Alpha       │  │ React Native│                      │
│  │ Next.js     │  │             │                      │
│  └─────────────┘  └─────────────┘                      │
│                                                         │
│  ┌─────────────┐  ┌─────────────┐                      │
│  │   [thumb]   │  │   [thumb]   │                      │
│  │ Finance     │  │ Portfolio   │                      │
│  │ Tracker     │  │ v2          │                      │
│  │ Vue.js      │  │ Svelte      │                      │
│  └─────────────┘  └─────────────┘                      │
│                                                         │
│  ⓘ 3 repositories were excluded based on config.       │
│                                                         │
│         ┌─────────────────────────────┐                 │
│         │  View My App Market →       │  ← 시안 버튼    │
│         └─────────────────────────────┘                 │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**핵심 요소**:
- 성공 체크 아이콘 (시안)
- 통계 카드: "NEW APPS ADDED: 12"
- 2열 그리드로 등록된 앱 카드 (큰 썸네일 + 앱명 + 프레임워크 태그)
- 제외된 레포 안내
- CTA: "View My App Market"

#### Step 4: Main 화면 (My Apps)

```
┌─────────────────────────────────────────────────────────┐
│  Aiden's Market                           🔄    +       │
├─────────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────────┐  │
│  │ ✓ Scan complete. 3 new apps found.               │  │
│  └───────────────────────────────────────────────────┘  │
│                                                         │
│  ┌─────────────────┐  ┌─────────────────┐              │
│  │                 │  │                 │              │
│  │    [thumb]      │  │    [thumb]      │              │
│  │                 │  │                 │              │
│  └─────────────────┘  └─────────────────┘              │
│                                                         │
│  ┌─────────────────┐  ┌─────────────────┐              │
│  │                 │  │                 │              │
│  │    [thumb]      │  │    [thumb]      │   ← 호버 시   │
│  │                 │  │                 │     오버레이  │
│  └─────────────────┘  └─────────────────┘              │
│                                                         │
│  ┌─────────────────┐  ┌─────────────────┐              │
│  │                 │  │                 │              │
│  │    [thumb]      │  │    [thumb]      │              │
│  │                 │  │                 │              │
│  └─────────────────┘  └─────────────────┘              │
│                                                         │
├─────────────────────────────────────────────────────────┤
│     📱 Apps        🔄 Sync        ⚙️ Settings           │
│                    ← 하단 네비게이션                     │
└─────────────────────────────────────────────────────────┘
```

**핵심 요소**:
- 헤더: "Aiden's Market" + 동기화/추가 아이콘
- 알림 배너: 스캔 완료 메시지 (시안 테두리)
- **2열 썸네일 그리드** (텍스트 없이 이미지만 표시)
- 호버 시 반투명 오버레이로 앱 정보 표시
- **하단 탭 바**: Apps | Sync | Settings

### 7.4 MVP 지원 URL 유형 (수동 입력 시)

| 유형 | 예시 | 설명 |
|------|------|------|
| **GitHub Pages** | `https://username.github.io/repo` | 정적 사이트 |
| **Vercel** | `https://app-name.vercel.app` | Next.js/React 앱 |
| **Netlify** | `https://app-name.netlify.app` | 정적/SSR 앱 |
| **Railway** | `https://app-name.up.railway.app` | 풀스택 앱 |
| **Render** | `https://app-name.onrender.com` | 웹서비스 |
| **Custom Domain** | `https://myapp.com` | 커스텀 도메인 |
| **Localhost** | `http://localhost:3000` | 로컬 개발 앱 |

### 7.5 MVP 기능 범위

#### 포함 (In Scope)

| 기능 | 설명 | 우선순위 |
|------|------|----------|
| **전체 레포 자동 스캔** | GitHub 연동 시 모든 레포 자동 분석 | P0 |
| **배포 URL 자동 탐지** | 각 레포별 배포 URL 자동 추출 | P0 |
| **앱 마켓 자동 배치** | 탐지된 앱을 마켓에 자동 등록 | P0 |
| **앱 정보 자동 생성** | 레포 이름, 설명, 썸네일 자동 설정 | P0 |
| **스캔 진행 상황 표시** | 실시간 스캔 상태 UI | P0 |
| **앱 런치** | URL 클릭 시 새 탭에서 열기 | P0 |
| **다시 스캔** | 수동으로 레포 재스캔 트리거 | P1 |
| **수동 앱 추가** | 자동 스캔 외 URL 직접 입력 | P1 |
| **즐겨찾기** | 별표 토글 | P1 |
| **앱 수정/삭제** | 기존 CRUD 활용 | P0 |

#### 제외 (Out of Scope - 이후 Phase)

| 기능 | Phase |
|------|-------|
| 카테고리 시스템 | Phase 1 |
| PWA/API/Docker 타입 | Phase 1 |
| Dashboard 모드 (타일 뷰) | Phase 2 |
| Store 모드 (사이드바) | Phase 3 |
| 상태 체크 (online/offline) | Phase 4 |
| 최근 사용 기록 | Phase 2 |

### 7.6 MVP 데이터 모델

기존 `projects` 테이블에 최소 필드만 추가:

```sql
-- MVP 마이그레이션 (최소 변경)
ALTER TABLE projects ADD COLUMN IF NOT EXISTS
  url TEXT,                        -- 앱 URL (필수)
  app_type TEXT DEFAULT 'web_app', -- MVP에서는 web_app만
  is_favorite BOOLEAN DEFAULT FALSE;

-- 인덱스
CREATE INDEX IF NOT EXISTS idx_projects_is_favorite ON projects(is_favorite);
```

### 7.7 MVP UI 변경

#### 앱 카드 변경 (v2.5 - 썸네일 중심 UI)

기본 상태에서는 **썸네일만 표시**, 마우스 호버 시 **오버레이로 상세 정보** 표시:

```
기본 상태 (썸네일만)              호버 상태 (오버레이)
+---------------------------+    +---------------------------+
|                           |    |  ┌─────────────────────┐  |
|     [썸네일 이미지]        |    |  │ 📱 앱 이름     [⭐] │  |
|                           |    |  │ 설명 (1줄)...       │  |
|                           | →  |  │                     │  |
|                           |    |  │ [🚀 앱 열기]       │  |
|                           |    |  └─────────────────────┘  |
+---------------------------+    +---------------------------+
     (클린한 그리드 뷰)              (호버 시 정보 표시)
```

**특징**:
- 기본: 썸네일 이미지만 표시 (앱 아이콘 느낌)
- 호버: 반투명 오버레이에 앱 이름, 설명, 열기 버튼 표시
- 클릭: 상세 페이지 이동
- 앱 열기 버튼: 새 탭에서 배포 URL 오픈

**구현**:
- `group` + `group-hover` 클래스 활용
- 오버레이: `bg-black/70` 반투명 배경
- 트랜지션: `transition-opacity duration-200`

#### 헤더 변경

```
기존                              MVP
+---------------------------+    +---------------------------+
|  Project Showcase Hub     |    |  Aiden's Market     [🔄] [+ 수동] |
+---------------------------+    +---------------------------+
                                       ↑         ↑
                                  다시 스캔   수동 앱 추가
```

### 7.8 MVP 파일 변경

#### 수정 필요

| 파일 | 변경 내용 |
|------|----------|
| `src/types/database.ts` | `url`, `app_type`, `is_favorite`, `github_repo` 필드 추가 |
| `src/contexts/auth-context.tsx` | GitHub 연동 후 자동 스캔 트리거 |
| `src/components/features/projects/project-card.tsx` | "앱 열기" 버튼, 즐겨찾기 토글 추가 |
| `src/hooks/use-projects.ts` | 즐겨찾기 토글, 벌크 생성 메서드 추가 |
| `src/app/(dashboard)/page.tsx` | 스캔 진행 상황 UI, 완료 알림 |

#### 신규 생성

| 파일 | 설명 |
|------|------|
| `src/lib/deployment-detector.ts` | 배포 URL 자동 탐지 유틸리티 |
| `src/lib/repo-scanner.ts` | 전체 레포 스캔 및 배치 로직 |
| `src/components/features/apps/scan-progress.tsx` | 스캔 진행 상황 표시 컴포넌트 |
| `src/components/features/apps/scan-results.tsx` | 스캔 완료 결과 표시 컴포넌트 |
| `src/components/features/apps/manual-add-dialog.tsx` | 수동 앱 추가 다이얼로그 |
| `src/hooks/use-repo-scanner.ts` | 레포 스캔 상태 관리 훅 |
| `src/app/api/github/scan-all/route.ts` | 전체 레포 스캔 API |
| `src/app/api/github/detect-deployment/route.ts` | 개별 레포 배포 URL 탐지 API |

### 7.9 MVP 구현 체크리스트

```
MVP 구현 순서:

1. [x] DB 마이그레이션
   - [x] url, app_type, is_favorite, github_repo 필드 추가
   - [x] 인덱스 생성

2. [x] 타입 정의 업데이트
   - [x] Project 인터페이스 확장
   - [x] ScanStatus, ScanResult 타입 정의

3. [x] 배포 URL 탐지 기능
   - [x] deployment-detector.ts 구현
     - [x] GitHub API (homepage, Pages, Environments)
     - [x] README 파싱 (Demo/Live 링크, 배지 URL)
     - [x] URL 유효성 검증 (HEAD 요청)
   - [x] detect-deployment API 엔드포인트

4. [x] 전체 레포 자동 스캔 기능
   - [x] repo-scanner.ts 구현
     - [x] 전체 레포 목록 조회
     - [x] 병렬 배포 URL 탐지
     - [x] 앱 자동 생성 (벌크 insert)
   - [x] scan-all API 엔드포인트
   - [x] use-repo-scanner.ts 훅 (상태 관리)

5. [x] 스캔 UI 컴포넌트
   - [x] scan-progress.tsx (진행 상황)
   - [x] scan-results.tsx (완료 결과)
   - [x] 메인 페이지에 스캔 상태 통합

6. [x] GitHub 연동 시 자동 스캔 트리거
   - [x] auth-context.tsx에 스캔 시작 로직
   - [x] 최초 연동 시 자동 실행
   - [x] "다시 스캔" 버튼

7. [x] 앱 카드 수정 (v2.5)
   - [x] 썸네일 중심 UI (호버 오버레이)
   - [x] "앱 열기" 버튼 추가
   - [ ] 즐겨찾기 토글 버튼 추가

8. [ ] 수동 앱 추가
   - [ ] manual-add-dialog.tsx
   - [ ] URL 직접 입력 폼

9. [x] 헤더 리브랜딩 (Project Showcase → Aiden's Market)

10. [x] 테스트
    - [x] GitHub 연동 → 자동 스캔 → 앱 배치 플로우
    - [x] 다시 스캔 동작
    - [x] 앱 열기 동작
    - [ ] 즐겨찾기 토글 동작
    - [ ] 수동 앱 추가 동작

11. [x] Stitch 디자인 시스템 적용 (v2.5)
    - [x] 다크 테마 기본 적용
    - [x] 시안 액센트 컬러
    - [x] 2열 썸네일 그리드
    - [x] 호버 오버레이 UI
```

### 7.10 MVP 완료 기준

| 기준 | 상세 |
|------|------|
| **자동 스캔** | GitHub 연동 시 모든 레포가 자동으로 스캔된다 |
| **자동 배치** | 배포된 앱이 발견되면 마켓에 자동 등록된다 |
| **스캔 상태** | 스캔 진행 상황이 실시간으로 표시된다 |
| **앱 실행** | 등록된 앱을 클릭하면 새 탭에서 열린다 |
| **다시 스캔** | 수동으로 레포를 재스캔할 수 있다 |
| **수동 추가** | 자동 스캔 외에 URL을 직접 입력하여 앱을 추가할 수 있다 |
| **즐겨찾기** | 앱을 즐겨찾기로 표시할 수 있다 |
| **CRUD** | 앱을 수정하고 삭제할 수 있다 |

---

## 8. Implementation Phases (Post-MVP)

> MVP 완료 후 진행하는 단계입니다.

### Phase 1: 데이터 모델 확장
- [ ] 카테고리 테이블 생성 및 초기 데이터
- [ ] app_type ENUM 생성 (web_app, pwa, api, docker)
- [ ] projects 테이블 추가 필드 (category_id, docker_image 등)
- [ ] TypeScript 타입 정의 업데이트
- [ ] 카테고리 API 구현

### Phase 2: Dashboard Mode
- [ ] 런처 뷰 컴포넌트 (app-tile, launcher-view)
- [ ] 즐겨찾기 섹션 (상단 고정)
- [ ] 최근 사용 기능 (app_usage 테이블, use-recent-apps 훅)
- [ ] Dashboard 메인 페이지

### Phase 3: Store Mode
- [ ] 카테고리 사이드바
- [ ] 앱 타입 필터 (web_app, pwa, api, docker)
- [ ] 태그 시스템
- [ ] Store 페이지 레이아웃

### Phase 4: 앱 실행 및 상태
- [ ] 앱 런처 유틸 (타입별 동작 분기)
- [ ] 상태 체크 API (HEAD 요청)
- [ ] 상태 뱃지 컴포넌트
- [ ] Docker 정보 표시

---

## 9. Success Metrics

| 지표 | 목표 |
|------|------|
| 등록된 앱 수 | 20+ |
| 일일 런치 횟수 | 10+ |
| 즐겨찾기 앱 수 | 5+ |
| Lighthouse Score | 90+ (Performance) |

---

## 10. Future Considerations

1. **다중 관리자**: 여러 명의 Admin 역할 지원
2. **그룹 관리**: 팀별 앱 컬렉션 기능
3. **위젯 시스템**: Homarr 스타일 대시보드 위젯
4. **자동 상태 체크**: Cron 기반 앱 상태 모니터링
5. **PWA 설치 안내**: PWA 앱 설치 가이드 제공
6. **Docker Compose 실행**: 원클릭 Docker 실행 (로컬 CLI 연동)
7. **댓글 알림**: 새 댓글/별점 알림 기능

---

## Appendix

### A. 파일 변경 목록

#### 수정 필요
| 파일 | 변경 내용 |
|------|----------|
| `src/types/database.ts` | App, Category 타입 추가 |
| `src/hooks/use-projects.ts` | useApps로 확장 |
| `src/components/features/projects/project-form.tsx` | 앱 필드 추가 |
| `src/components/features/projects/project-card.tsx` | 상태/런치 버튼 |
| `src/components/features/views/view-mode-switcher.tsx` | launcher 모드 |
| `src/components/layout/dashboard-header.tsx` | Aiden's Market 브랜딩 |
| `src/app/(dashboard)/page.tsx` | Dashboard 페이지 |
| `src/app/api/projects/route.ts` | 카테고리/필터 |

#### 신규 생성
```
src/components/features/apps/
├── app-tile.tsx
├── status-badge.tsx
└── docker-info.tsx

src/components/features/store/
├── category-sidebar.tsx
└── app-filters.tsx

src/components/features/views/
└── launcher-view.tsx

src/hooks/
├── use-categories.ts
├── use-favorites.ts
└── use-recent-apps.ts

src/lib/
└── app-launcher.ts

src/app/(dashboard)/store/
└── page.tsx

src/app/api/categories/
└── route.ts

supabase/migrations/
└── 005_app_market.sql
```

### B. 디자인 파일 (Stitch)

> **위치**: `design/stitch_welcome_to_aiden_s_market.zip`

| 화면 | 파일 경로 | 설명 |
|------|----------|------|
| Welcome | `welcome_to_aiden's_market/` | 온보딩, GitHub 연동 |
| Scanning | `scanning_repositories/` | 스캔 진행 중 UI |
| Scan Complete | `scan_complete!/` | 스캔 완료, 결과 표시 |
| My Apps (Main) | `my_apps_(main)/` | 메인 앱 그리드 |

각 폴더 구성:
- `screen.png` - 디자인 스크린샷
- `code.html` - Stitch 생성 HTML 코드

#### 디자인 컬러 팔레트

```css
:root {
  /* Background */
  --bg-primary: #0a1628;      /* 메인 배경 */
  --bg-card: #1a2a3a;         /* 카드 배경 */
  --bg-overlay: rgba(0,0,0,0.7); /* 호버 오버레이 */

  /* Accent */
  --accent-cyan: #00d4ff;     /* 주요 액센트 */
  --accent-success: #22c55e;  /* 성공 체크 */

  /* Text */
  --text-primary: #ffffff;
  --text-secondary: #94a3b8;
  --text-muted: #64748b;
}
```

### C. 참고 자료

- [Homer Dashboard](https://github.com/bastienwirtz/homer)
- [Homarr](https://homarr.dev/)
- [Heimdall](https://heimdall.site/)
- [Dashy](https://dashy.to/)
- [awesome-selfhosted](https://github.com/awesome-selfhosted/awesome-selfhosted)
- [PWA Market](https://github.com/openhomescreen/pwa-market)
- [Cloudron](https://www.cloudron.io/)
